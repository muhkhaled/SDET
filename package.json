{
    "scripts": {
    "frontend:install": "cd UI_Tests && npm install",
    "frontend:test": "cd UI_Tests && npm run test",
    "api:install": "cd API_Tests && npm install",
    "api:test": "cd API_Tests && npm run test"
}

}